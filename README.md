# Treason Portal

# Setup
This app was setup with a config/config.env and Makefile to run the code easier by setting env
variables and exposing make run to load them up.

- npm install
- make init
- make run
